{
    "name": "ZBSystems Inventory",
    "version": "2.0.2",
    "private": true,
    "scripts": {
        "install": "npm run install --prefix AFSystem && npm run install --prefix af-scanner",
        "start": "npm run start --prefix AFSystem && npm run start --prefix af-scanner"
    },
    "dependencies": {
        "node-fetch": "^3.3.2"
    }
}
